# Core
python-jobspy>=1.1.82
playwright>=1.44.0
groq
requests
pyyaml

# AI / PDF
weasyprint

# Excel tracking (no cloud account needed)
openpyxl

# Google Sheets (OPTIONAL — only used if GSHEET_CREDENTIALS_JSON is set)
gspread
google-auth
google-auth-oauthlib
google-api-python-client

# Review UI (approve-before-send)
fastapi
uvicorn

# Email
# (uses built-in smtplib + imaplib — no extra package needed)

# Testing
pytest
